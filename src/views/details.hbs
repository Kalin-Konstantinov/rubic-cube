<main>
  <h1>{{ctx.name}}</h1>
  <img class="cube"
    src="{{ctx.imageUrl}}">
  <div class="details">
    <p><span>Description:</span>{{ctx.description}}</p>
    <p><span>Difficulty level:</span> {{ctx.difficultyLevel}}</p>
    <a class="btn" href="/">Back</a>
  </div>
</main>